const con = require('../config/db');
const { userList } = require('../models/user')
module.exports = {
    userList: {
        get: (req,res) => {
            try {
                
            } catch (error) {
                
            }
        }
    }
}